import jade.Window;

public class Main {
    public static void main(String[] args) {
        // Creates a window
        Window window = Window.get();
        // Runs the window
        window.run();
    }
}
